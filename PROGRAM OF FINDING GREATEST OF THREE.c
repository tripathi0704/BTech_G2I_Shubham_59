#include<stdio.h>
int main()
{
    int x,y,z;
    printf(" ENTER THE THREE NUMBER\n");
    scanf(" %d %d %d",&x ,&y ,&z);
    if (a >= b && a >= c)
    {
        printf(" great number is a %d",a);
    } 
    else if (b >= c && b >= a)
    {
        printf(" great number is b %d", b);

    }
    else (c >= a && c >= b)
    {
        printf(" great number is c %d" ,c);
    }
}